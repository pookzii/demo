pipeline {
    agent any
    environment {
        APP_NAME = "pookzii and O"
    }
    stages {
        stage('Build Image'){
            steps{
                sh "echo ${env.APP_NAME}"
            }
        }
    }
}
