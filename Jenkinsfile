pipeline {
    agent any
    environment {
        APP_NAME = "pookzii beautiful"
    }
    stages {
        stage('Build Image'){
            steps{
                sh "echo ${env.APP_NAME}"
            }
        }
    }
}